Link this file to ~/.config/terminator/config
```bash
ln -sf $PWD/config ~/.config/terminator/config
```

I had to open graphical "Profile" config and select Palette as "Solarized"
instead of "custom" for it to work. Not sure why.
